MONGODB_URI=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/gj_pfm?retryWrites=true&w=majority
NODE_ENV=development
JWT_SECRET=your-super-secret-jwt-key-change-in-production!!!  
FRONTEND_URL=http://localhost:5173
PORT=5000

# Get FREE MongoDB Atlas URI:
# 1. https://www.mongodb.com/atlas/database → Sign up → Create M0 cluster
# 2. Network Access → Add IP 0.0.0.0/0  
# 3. Database Users → Create user → Get connection string
# 4. Copy above MONGODB_URI → replace username/password/cluster → create .env → restart backend

# Local MongoDB (macOS):
# brew tap mongodb/brew && brew install mongodb-community && brew services start mongodb/brew/mongodb-community
# MONGODB_URI=mongodb://localhost:27017/gj_pfm
